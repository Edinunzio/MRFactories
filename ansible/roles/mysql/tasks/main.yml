---
- name: Install packages
  apt: name={{ item }}
  with_items:
    - mysql-server
    - libmysqlclient-dev
    - python-pip
    - python-dev

- name: Install required Python modules
  pip: name=MySQL-python

- name: Enable and start mysql
  service: name=mysql enabled=yes state=started

- name: Create mysql user
  mysql_user: name=sa password=dinz217 priv=*.*:ALL

- name: Create mysql database
  mysql_db: name=MR

#- name: Create config/database.yml
  #template: src=database.yml.j2 dest=virtualbox/config/database.yml owner={{ user_name }} mode=0600 backup=yes
  #when: mysql_orm in ['activerecord', 'sequel', 'datamapper']
